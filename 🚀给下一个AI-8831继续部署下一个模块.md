# 🚀 给下一个AI - 在8831继续部署下一个模块

**复制下面的提示词给新的Cursor会话**

---

## 📋 给AI的提示词（完整版）

```
你好！我需要在8831测试环境继续部署任务所·Flow的下一个模块。

【背景】
- 项目：任务所·Flow v1.9
- 当前环境：Mac
- 测试端口：8831（dashboard-test-8831/）
- 生产端口：8820（dashboard-v1.9-20251121/）
- API端口：8800（必须运行）

【刚完成的工作】
透视塔模块已成功部署到8820生产环境，包括：
1. 统计数字动态更新（161、24、15、12）
2. 5个Tab动态渲染（严格按8820原格式）
3. 架构师扫描Tab（新增）
4. 模块独立滚动

【核心经验】
1. ✅ 先在8831测试，验证通过后手动cp到8820
2. ✅ 绝不直接修改8820，绝不用Python脚本
3. ✅ 必须先读取8820原格式，严格匹配
4. ✅ 先curl测试API，确认字段名和端点
5. ✅ 每次修改都要备份
6. ✅ 浏览器必须强制刷新（Command+Shift+R）

【7个关键坑及解决方案】
1. API字段名：用features/issues/suggestions，不是data
2. API端点：查看start_insight_api.py确认路由
3. 格式匹配：读取8820原HTML，复制CSS类和DOM结构
4. Tab按钮数字：更新.tab-count，不只是统计卡片
5. 多余标签：检查closing div，删除多余的
6. 滚动样式：在main-content层，不在tab-panel
7. 模块关闭：Tab结束后加2-3个closing div关闭模块

【必读文档】
请先读取这些文件：
1. 📚透视塔部署完整经验-Mac借鉴Windows.md（7个问题详解）
2. 🎊透视塔部署完成报告-8820生产环境.md（技术细节）

【当前要做的】
请帮我在8831测试环境部署[模块名称]，具体要求：
1. 先读取8820中该模块的原HTML结构
2. 了解该模块的API端点和数据格式
3. 在8831开发和测试
4. 验证通过后，我会告诉你是否部署到8820

【禁止操作】
❌ 不要直接修改8820
❌ 不要使用Python脚本批量替换
❌ 不要假设API字段名
❌ 不要用简化格式替代原格式
❌ 不要在tab-panel上加滚动样式

【必须操作】
✅ 先在8831测试
✅ 先curl测试API
✅ 先读8820原格式
✅ 每步都要验证
✅ 修改后强制刷新
```

---

## 🎯 提示词使用方法

### Step 1: 复制上面的完整提示词

### Step 2: 打开新的Cursor会话

### Step 3: 粘贴提示词，并补充：
```
【当前要做的】
请帮我在8831测试环境部署[模块名称]，具体要求：
[你的具体需求]
```

例如：
- "部署待开发任务池的动态数据功能"
- "部署架构师工作台的实时监控"
- "部署全栈工程师模块的任务列表"
- "部署记忆空间的搜索功能"

---

## 📝 快速启动命令

### 启动8831测试环境
```bash
cd "/Users/yalinwang/Desktop/任务所 1.8/taskflow-v1-2/taskflow-v1-2/dashboard-test-8831"
python3 -m http.server 8831 &
```

### 访问测试
```
http://localhost:8831
```

### 部署到8820（测试通过后）
```bash
cd "/Users/yalinwang/Desktop/任务所 1.8/taskflow-v1-2/taskflow-v1-2"

# 备份
cd dashboard-v1.9-20251121
cp index.html "index.html.backup-$(date +%Y%m%d-%H%M%S)"

# 复制
cd ..
cp dashboard-test-8831/index.html dashboard-v1.9-20251121/index.html

# 重启8820
kill -9 $(lsof -ti :8820)
cd dashboard-v1.9-20251121
python3 -m http.server 8820 &
```

---

## 🎓 给AI的关键提醒

1. **Ultra Memory记忆ID**:
   - mem_1763727558109_ysd7ei88f（滚动条血泪经验）
   - mem_1763726001849_4ezx4c7q2（透视塔5个坑）
   - mem_1763725234017_fc80nxhnh（透视塔API部署）

2. **搜索记忆关键词**:
   - "透视塔部署完整经验"
   - "滚动条血泪经验"
   - "Dashboard黄金规则"

3. **重要文件位置**:
   - 生产环境：dashboard-v1.9-20251121/index.html（8820）
   - 测试环境：dashboard-test-8831/index.html
   - API服务：start_insight_api.py（8800）

---

**这个提示词包含了所有成功经验，可以用于部署任何模块！** 🚀






