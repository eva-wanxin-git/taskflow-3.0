# 📋 架构师模块完整部署指南 - 8831到8820

**部署日期**: 2025-11-21  
**状态**: ✅ 测试通过  
**环境**: Mac + 8831测试 + 8820生产  

---

## 🎯 部署目标

将架构师工作台模块的**硬编码演示数据**改为**动态加载真实数据**，包括：
- 事件流（174个真实事件）
- 对话历史（11个对话记录）
- 监控数据（Token使用、项目状态）

**核心要求**：
✅ 严格保持8820的UI样式不变  
✅ 只修改数据源，不改HTML结构  
✅ 去除emoji图标（黑白线形风格）  
✅ 左右两侧都有独立滚动条（微信式）  

---

## 📋 完整部署步骤

### Step 1: API端点开发（手动）

**文件**: `start_insight_api.py`

**添加3个架构师端点**：

```python
@app.get("/api/architect/events")
async def get_architect_events():
    """获取架构师事件流"""
    data = load_json_file(DATA_DIR / "architect_events.json")
    events = data.get("events", [])
    return {
        "success": True,
        "total": len(events),
        "events": events,
        "updated_at": datetime.now().isoformat()
    }

@app.get("/api/architect/monitor")
async def get_architect_monitor():
    """获取架构师监控数据"""
    data = load_json_file(DATA_DIR / "architect_monitor.json")
    return {
        "success": True,
        "data": data,
        "updated_at": datetime.now().isoformat()
    }

@app.get("/api/architect/conversations")
async def get_architect_conversations():
    """获取架构师对话历史"""
    data = load_json_file(DATA_DIR / "architect-conversations.json")
    sessions = data.get("sessions", [])
    stats = data.get("stats", {})
    return {
        "success": True,
        "total": len(sessions),
        "sessions": sessions,
        "stats": stats,
        "updated_at": datetime.now().isoformat()
    }
```

**测试API**：
```bash
curl http://localhost:8800/api/architect/events
curl http://localhost:8800/api/architect/monitor
curl http://localhost:8800/api/architect/conversations
```

---

### Step 2: 备份8831文件

```bash
cd dashboard-test-8831
cp index.html "index.html.backup-architect-$(date +%Y%m%d-%H%M%S)"
```

---

### Step 3: 清除HTML硬编码数据

**事件流Tab**（9268-9341行）：
- 删除3个`<div class="architect-timeline-item">`演示事件
- 保留筛选按钮
- 添加"正在加载事件流..."占位符

**对话历史Tab**（9335-9377行）：
- 删除3个`<div class="architect-conversation-item">`演示对话
- 添加"正在加载对话历史..."占位符

**右侧消息详情**（9354-9412行）：
- 删除所有`<div class="architect-message">`演示消息
- 添加"正在加载对话详情..."占位符

---

### Step 4: 添加JavaScript动态加载代码

**位置**: 在`// ========== 待开发任务模块函数 ==========`之前（约15287行）

**完整代码**（约200行）：

```javascript
// ========== 架构师模块动态数据加载 ==========
// 全局变量：保存对话数据
let architectConversationsData = [];

async function loadArchitectData() {
    try {
        console.log('📡 开始加载架构师数据...');
        
        // 加载监控数据
        const monitorResponse = await fetch('http://localhost:8800/api/architect/monitor');
        const monitorData = await monitorResponse.json();
        if (monitorData.success) {
            updateArchitectMonitor(monitorData.data);
        }

        // 加载事件流
        const eventsResponse = await fetch('http://localhost:8800/api/architect/events');
        const eventsData = await eventsResponse.json();
        if (eventsData.success) {
            updateArchitectEvents(eventsData.events);
        }

        // 加载对话历史
        const conversationsResponse = await fetch('http://localhost:8800/api/architect/conversations');
        const conversationsData = await conversationsResponse.json();
        if (conversationsData.success) {
            updateArchitectConversations(conversationsData.sessions);
        }
        
        console.log('🎉 架构师数据全部加载完成！');
    } catch (error) {
        console.error('❌ 加载架构师数据失败:', error);
    }
}

function updateArchitectMonitor(data) {
    // 更新Token、状态、事件数
    const statItems = document.querySelectorAll('.architect-quick-stats .architect-stat-item');
    if (statItems.length >= 3) {
        statItems[0].querySelector('.architect-stat-value').textContent = 
            `${Math.round(data.token_usage.used / 1000)}K`;
    }
}

function updateArchitectEvents(events) {
    const timelineContainer = document.querySelector('.architect-event-timeline');
    if (!timelineContainer) return;
    
    // 保留筛选按钮
    const filterContainer = timelineContainer.querySelector('.architect-timeline-filters');
    timelineContainer.innerHTML = '';
    timelineContainer.appendChild(filterContainer);
    
    // 最近20个事件
    const recentEvents = events.slice(-20).reverse();
    
    recentEvents.forEach(event => {
        const eventItem = document.createElement('div');
        eventItem.className = 'architect-timeline-item';
        
        // 完全按照8820格式（不含emoji）
        let eventHTML = `
            <div class="architect-timeline-marker"></div>
            <div class="architect-event-card">
                <div class="architect-event-header">
                    <div>
                        <div class="architect-event-title">${event.content}</div>
                        <div class="architect-event-meta">AI Architect · ${event.type}</div>
                    </div>
                    <div class="architect-event-time">${event.timestamp}</div>
                </div>`;
        
        // 添加描述和标签（如果有）
        if (event.metadata && event.metadata.description) {
            eventHTML += `<div class="architect-event-description">${event.metadata.description}</div>`;
        }
        if (event.metadata && event.metadata.tags) {
            eventHTML += `<div class="architect-event-tags">`;
            event.metadata.tags.forEach(tag => {
                eventHTML += `<span class="architect-event-tag">${tag}</span>`;
            });
            eventHTML += `</div>`;
        }
        
        eventHTML += `</div>`;
        eventItem.innerHTML = eventHTML;
        timelineContainer.appendChild(eventItem);
    });
    
    // 更新徽章数量
    const statItems = document.querySelectorAll('.architect-quick-stats .architect-stat-item');
    if (statItems.length >= 3) {
        statItems[2].querySelector('.architect-stat-value').textContent = events.length;
    }
    
    const eventsTab = document.querySelector('.architect-tab-item[onclick*="events"] .architect-tab-badge');
    if (eventsTab) eventsTab.textContent = events.length;
}

function updateArchitectConversations(sessions) {
    const conversationList = document.querySelector('.architect-conversation-items');
    if (!conversationList) return;
    
    architectConversationsData = sessions;
    conversationList.innerHTML = '';
    
    sessions.forEach((session, index) => {
        const isActive = index === 0 ? 'active' : '';
        const conversationItem = document.createElement('div');
        conversationItem.className = `architect-conversation-item ${isActive}`;
        
        // ✅ 使用addEventListener正确传递参数
        conversationItem.addEventListener('click', function(e) {
            window.selectArchitectConversation(index, this);
        });
        
        conversationItem.innerHTML = `
            <div class="architect-conversation-item-header">
                <div class="architect-conversation-item-title">${session.title}</div>
                <div class="architect-conversation-item-time">${session.updated_at || session.last_active}</div>
            </div>
            <div class="architect-conversation-item-preview">${session.summary}</div>
            <div class="architect-conversation-item-meta">
                <span>${session.messages_count}条消息</span>
                <span>${Math.round(session.total_tokens / 1000)}K tokens</span>
            </div>`;
        
        conversationList.appendChild(conversationItem);
    });
    
    // 默认显示第一个
    if (sessions.length > 0) {
        window.updateConversationDetail(sessions[0]);
    }
    
    // 更新徽章
    const conversationsTab = document.querySelector('.architect-tab-item[onclick*="conversations"] .architect-tab-badge');
    if (conversationsTab) conversationsTab.textContent = sessions.length;
}

// ✅ 定义为全局函数
window.updateConversationDetail = function(session) {
    const detailTitle = document.querySelector('.architect-detail-title');
    const detailMeta = document.querySelector('.architect-detail-meta');
    const messagesContainer = document.querySelector('.architect-messages-container');
    
    if (!detailTitle || !detailMeta || !messagesContainer) return;
    
    detailTitle.textContent = session.title;
    detailMeta.innerHTML = `
        <span>${session.updated_at.replace('T', ' ').substring(0, 16)}</span>
        <span>${session.messages_count}条消息</span>
        <span>${Math.round(session.total_tokens / 1000)}K tokens</span>`;
    
    // 生成15条完整聊天消息（针对conv-004）
    let messagesHTML = '';
    if (session.session_id === 'conv-004') {
        // 15条完整消息气泡...
    } else {
        // 其他对话显示摘要
        messagesHTML = `<div style="padding: 40px; text-align: center;">
            <div style="font-size: 14px; color: var(--noir-silver);">${session.summary}</div>
        </div>`;
    }
    
    messagesContainer.innerHTML = messagesHTML;
};

window.selectArchitectConversation = function(index, element) {
    document.querySelectorAll('.architect-conversation-item').forEach(item => {
        item.classList.remove('active');
    });
    if (element) element.classList.add('active');
    
    if (architectConversationsData[index]) {
        window.updateConversationDetail(architectConversationsData[index]);
    }
};

// 页面加载时自动执行
document.addEventListener('DOMContentLoaded', function() {
    loadArchitectData();
});
if (document.readyState !== 'loading') {
    loadArchitectData();
}
```

---

## 🔧 滚动条修复（血泪经验）

**核心CSS设置**（必须全部加上）：

```css
.architect-tab-pane.active {
    display: flex;
    flex-direction: column;
    min-height: 0;  /* ✅ 关键1 */
}

.architect-conversation-layout {
    flex: 1;
    display: flex;
    min-height: 0;  /* ✅ 关键2 */
    overflow: hidden;
}

.architect-conversation-list {
    width: 380px;
    display: flex;
    flex-direction: column;
    min-height: 0;  /* ✅ 关键3 */
}

.architect-conversation-items {
    flex: 1;
    overflow-y: auto;  /* ✅ 左侧滚动条 */
    min-height: 0;  /* ✅ 关键4 */
}

.architect-conversation-detail {
    flex: 1;
    display: flex;
    flex-direction: column;
    min-height: 0;  /* ✅ 关键5 */
    overflow: hidden;
}

.architect-messages-container {
    flex: 1;
    overflow-y: auto;  /* ✅ 右侧滚动条 */
    min-height: 0;  /* ✅ 关键6 */
    padding: var(--space-6);
}

.architect-conversation-search {
    flex-shrink: 0;  /* ✅ 搜索框不参与滚动 */
}
```

**原理**：4层嵌套结构，每层都要`min-height: 0`，否则flex子元素无法滚动。

---

## 🚀 自动化功能

### 1️⃣ 对话记录自动保存API

**端点**: `POST http://localhost:8800/api/architect/save-conversation`

**参数**:
```json
{
  "title": "对话标题",
  "total_tokens": 50000,
  "messages_count": 25,
  "summary": "对话摘要（200字以内）",
  "tags": ["tag1", "tag2"],
  "status": "completed",  // 可选：active/completed
  "project_id": "TASKFLOW"  // 可选
}
```

**使用方法**:
```bash
# 在对话中告诉AI
"保存这个对话，标题是'XXX功能开发讨论'，摘要是'...'"

# 或直接curl
curl -X POST http://localhost:8800/api/architect/save-conversation \
  -H "Content-Type: application/json" \
  -d '{"title": "...", "total_tokens": 50000, "messages_count": 25}'
```

**返回**:
```json
{
  "success": true,
  "message": "对话记录已保存",
  "session_id": "conv-011"
}
```

---

### 2️⃣ 事件流自动监听脚本

**文件**: `scripts/auto_monitor_architect_events.py`

**功能**: 监听文件变化，自动添加事件到`architect_events.json`

**监听的目录**:
- `docs/reports/` - 完成报告、审查报告
- `docs/arch/` - 架构文档
- 项目根目录 - 部署脚本

**自动识别的事件类型**:

| 文件特征 | 事件类型 | 示例 |
|---------|---------|-----|
| 包含"完成报告" | task_complete ✅ | REQ-005-完成报告.md |
| 包含"审查报告" | code_review 🔍 | 架构师审查-REQ-005.md |
| 包含"交接" | handoff 🤝 | 交接-透视塔部署.md |
| *.bat / *.sh / 包含"启动" | deployment 🚀 | 启动8820-Dashboard.bat |
| 包含"测试"/"验证" | test 🧪 | Dashboard功能测试.md |

**启动监听器**:
```bash
cd "/Users/yalinwang/Desktop/任务所 1.8/taskflow-v1-2/taskflow-v1-2"

# 后台运行
./启动事件监听器.sh &

# 查看进程
ps aux | grep auto_monitor

# 停止监听
pkill -f auto_monitor_architect_events.py
```

**工作流程**:
```
文件创建/修改
↓
监听器检测（5秒冷却）
↓
判断事件类型
↓
自动添加到architect_events.json
↓
Dashboard刷新后自动显示
```

---

## ⚠️ 常见问题和解决方案

### 问题1: 点击左侧对话，右侧内容不切换

**原因**: 有两个`selectArchitectConversation`函数定义冲突

**位置**: 
- 旧函数约15016行（window.selectArchitectConversation）
- 新函数约15373行

**解决**: 删除旧函数定义，只保留新的全局函数

---

### 问题2: 左侧对话列表没有滚动条

**原因**: `overflow-y: auto`设置在错误的元素上

**错误设置**:
```css
.architect-conversation-list { overflow-y: auto; }  /* ❌ 错误 */
```

**正确设置**:
```css
.architect-conversation-items { overflow-y: auto; }  /* ✅ 正确 */
```

**必须配合**:
```css
.architect-conversation-list { min-height: 0; }
.architect-conversation-items { min-height: 0; }
.architect-conversation-search { flex-shrink: 0; }
```

---

### 问题3: 右侧消息详情没有滚动条

**原因**: 缺少`min-height: 0`

**必须每一层都加**:
```css
.architect-tab-pane.active { min-height: 0; }
.architect-conversation-layout { min-height: 0; }
.architect-conversation-detail { min-height: 0; }
.architect-messages-container { min-height: 0; }
```

---

### 问题4: 事件显示emoji图标（🚀📝等）

**原因**: 使用了`${event.icon}`

**错误代码**:
```javascript
<div class="architect-event-title">${event.icon || '📝'} ${event.content}</div>
```

**正确代码**:
```javascript
<div class="architect-event-title">${event.content}</div>
```

**原则**: 系统只用黑白线形图标，不用彩色emoji

---

### 问题5: 字体样式与8820不一致

**必须严格遵循**:
```css
.architect-event-title {
    font-size: var(--text-base);        /* 不是text-sm */
    font-weight: var(--weight-semibold); /* 不是weight-normal */
    color: var(--noir-charcoal);
}
```

---

## 📊 数据文件结构

### architect_events.json
```json
{
  "events": [
    {
      "id": "event-001",
      "timestamp": "2025-11-21 18:00:00",
      "type": "deployment",
      "content": "部署完成: 架构师模块动态数据集成",
      "metadata": {
        "description": "详细描述...",
        "tags": ["部署", "架构师模块"]
      }
    }
  ]
}
```

### architect_monitor.json
```json
{
  "token_usage": {
    "used": 299000,
    "total": 1000000,
    "percentage": 29.9
  },
  "status": {
    "text": "✅ 工作完成",
    "current_task": "..."
  },
  "project_info": {
    "name": "任务所·Flow v1.7",
    "completion": 60.0
  }
}
```

### architect-conversations.json
```json
{
  "sessions": [
    {
      "session_id": "conv-004",
      "title": "架构师模块动态数据部署到8831",
      "total_tokens": 137000,
      "messages_count": 38,
      "summary": "摘要...",
      "tags": ["deployment", "architect-module"]
    }
  ],
  "stats": {
    "total_conversations": 11,
    "total_tokens": 768000
  }
}
```

---

## 🧪 验证清单

部署完成后，必须验证：

### 事件流Tab
- [ ] 显示174个真实事件
- [ ] 没有emoji图标
- [ ] 字体大小和粗细正确
- [ ] Tab徽章显示"174"
- [ ] 有描述和标签（如果数据中有）

### 对话历史Tab
- [ ] 左侧显示11个对话记录
- [ ] **左侧有滚动条**（可上下滚动）
- [ ] 点击任意对话，右侧立即切换
- [ ] 右侧显示完整标题、时间、统计
- [ ] 点击conv-004显示15条完整聊天消息
- [ ] **右侧有滚动条**（可上下滚动）
- [ ] Tab徽章显示"11"

### 监控数据
- [ ] Token显示：299K
- [ ] 提示词版本：v4.0
- [ ] 事件数量：174
- [ ] 状态徽章：✅ 工作完成
- [ ] 进度：60%

### 认命指令Tab
- [ ] 静态内容正常显示
- [ ] 可以滚动查看完整提示词
- [ ] 复制按钮正常工作

---

## 🚀 部署到8820生产环境

**测试通过后，执行部署**：

```bash
cd "/Users/yalinwang/Desktop/任务所 1.8/taskflow-v1-2/taskflow-v1-2"

# 1. 备份8820
cd dashboard-v1.9-20251121
cp index.html "index.html.backup-before-architect-$(date +%Y%m%d-%H%M%S)"

# 2. 复制8831到8820
cd ..
cp dashboard-test-8831/index.html dashboard-v1.9-20251121/index.html

# 3. 重启8820
kill -9 $(lsof -ti :8820)
cd dashboard-v1.9-20251121
python3 -m http.server 8820 &

# 4. 验证
open http://localhost:8820
```

**强制刷新**: Command + Shift + R

---

## 💡 使用技巧

### 保存当前对话
```
用户："保存这个对话到对话历史，标题是'XXX模块部署讨论'"
AI会自动调用API保存
```

### 自动记录部署事件
```bash
# 创建完成报告
echo "..." > docs/reports/架构师模块-完成报告.md

# 监听器自动记录
✅ 事件已记录: 任务完成报告: 架构师模块-完成报告.md

# Dashboard自动显示新事件
```

---

## 📝 核心经验总结

1. ✅ **先在8831测试，验证通过后手动cp到8820**
2. ✅ **绝不直接修改8820，绝不用Python脚本批量替换**
3. ✅ **必须先读取8820原格式，严格匹配**
4. ✅ **每层都要加min-height:0，否则滚动条不生效**
5. ✅ **定义全局函数用window.xxx，避免冲突**
6. ✅ **addEventListener比onclick更可靠**
7. ✅ **浏览器必须强制刷新（Command+Shift+R）**

---

**架构师模块部署完成！** 🎉

**下一步**: 可以用同样的方法部署其他模块（全栈工程师、待开发任务池等）

