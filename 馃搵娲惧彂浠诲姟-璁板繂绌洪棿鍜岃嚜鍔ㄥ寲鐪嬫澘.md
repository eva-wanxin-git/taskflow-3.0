# 📋 派发任务 - Dashboard记忆空间Tab + 自动化看板刷新

**派发时间**: 2025-11-19 12:00  
**派发人**: AI Architect (Expert Level)  
**任务数量**: 3个（8小时）

---

## 🎯 检查结果

### 已检查的3个功能

| 功能 | 检查结果 | 代码位置 | 状态 |
|------|---------|---------|------|
| Dashboard事件流Tab | ✅ 已存在 | templates.py line 2725 | 基础版已实现 |
| Dashboard记忆空间Tab | ❌ 未实现 | - | 需要新建 |
| 自动化看板刷新脚本 | ❌ 未实现 | - | 需要新建 |

### 结论

- **事件流Tab**: 已有基础版本，可以增强（优先级P2）
- **记忆空间Tab**: 完全缺失，API已完整，只需UI（优先级P1）⭐
- **自动化看板**: 完全缺失，避免手动更新（优先级P1）⭐

---

## 📋 新创建的任务（已录入数据库）

### 任务1: TASK-UI-001 - Dashboard项目记忆空间Tab ⭐

**优先级**: P1（重要）  
**预估工时**: 3小时  
**执行者**: 全栈工程师·李明  
**依赖**: 无（API已完整，可以立即开始）

#### 任务背景

- ✅ REQ-002项目记忆空间已完成
- ✅ ProjectMemoryService已实现
- ✅ API已完整（11个端点，762行代码）
- ❌ Dashboard没有UI展示

**就差最后一个UI界面了！**

#### 需要实现的UI功能

**Tab标题**: "📚 项目记忆空间"

**Tab内容区域**:

1. **记忆列表**（主区域）
   ```html
   <div class="memory-list">
     <div class="memory-item">
       <div class="memory-header">
         <span class="memory-type">[ultra]</span>
         <span class="memory-category">知识</span>
         <h4>React Hooks 最佳实践</h4>
         <span class="memory-importance">⭐⭐⭐⭐⭐⭐⭐</span>
       </div>
       <div class="memory-content">
         使用 useCallback 优化性能...
       </div>
       <div class="memory-meta">
         <span>创建时间: 2025-11-18</span>
         <span>创建者: AI Architect</span>
         <span>标签: react, hooks, performance</span>
       </div>
     </div>
   </div>
   ```

2. **筛选器**（侧边栏或顶部）
   - 按类型：session/ultra/decision/solution
   - 按分类：architecture/problem/solution/decision/knowledge
   - 按重要性：1-10
   - 按标签：显示所有标签，可多选

3. **创建记忆按钮**
   ```html
   <button onclick="showCreateMemoryForm()">
     ➕ 新建记忆
   </button>
   ```

4. **创建表单**（弹窗或展开）
   - 标题（输入框）
   - 内容（文本域）
   - 类型（下拉：session/ultra/decision/solution）
   - 分类（下拉：architecture/problem等）
   - 标签（输入，逗号分隔）
   - 重要性（滑块，1-10）

5. **搜索框**
   ```html
   <input type="text" placeholder="搜索记忆..." 
          onkeyup="searchMemories(this.value)">
   ```

#### API调用示例

```javascript
// 加载记忆列表
async function loadMemories(filters = {}) {
    const params = new URLSearchParams();
    if (filters.category) params.append('category', filters.category);
    if (filters.memory_type) params.append('memory_type', filters.memory_type);
    if (filters.tags) params.append('tags', filters.tags);
    params.append('limit', 50);
    
    const response = await fetch(
        `http://localhost:8800/api/projects/TASKFLOW/memories?${params}`
    );
    const data = await response.json();
    renderMemories(data.memories);
}

// 创建记忆
async function createMemory(memoryData) {
    const response = await fetch(
        'http://localhost:8800/api/projects/TASKFLOW/memories',
        {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify(memoryData)
        }
    );
    return await response.json();
}

// 跨会话知识继承
async function inheritKnowledge(context) {
    const response = await fetch(
        `http://localhost:8800/api/projects/TASKFLOW/knowledge/inherit?context=${context}&limit=20`
    );
    const data = await response.json();
    // data包含: architectural_decisions, problem_solutions, recent_memories
    return data;
}
```

#### 验收标准

- [ ] Tab可以切换显示
- [ ] 记忆列表正常显示
- [ ] 筛选器工作正常
- [ ] 搜索功能正常
- [ ] 可以创建新记忆
- [ ] 可以查看记忆详情
- [ ] UI风格符合工业美学（黑白红配色）

#### 参考实现

- 参考其他Tab的实现方式（任务清单Tab、对话历史Tab）
- 参考API文档：http://localhost:8800/api/docs

---

### 任务2: TASK-AUTO-001 - 自动化看板刷新脚本 ⭐

**优先级**: P1（重要）  
**预估工时**: 2小时  
**执行者**: 全栈工程师·李明  
**依赖**: 无（可以立即开始）

#### 任务背景

**核心问题**: 功能完成但看板不同步，团队鬼打墙！

**解决方案**: 自动化脚本定期同步看板

#### 需要创建的脚本

**新建文件**: `services/task_board_auto_sync.py`

#### 脚本功能

```python
#!/usr/bin/env python3
"""
自动化看板同步脚本
从数据库和事件流拉取最新状态，自动更新task-board.md
"""
import sqlite3
from pathlib import Path
from datetime import datetime
import re

class TaskBoardAutoSync:
    """看板自动同步器"""
    
    def __init__(self, db_path, board_path):
        self.db_path = db_path
        self.board_path = Path(board_path)
        self.backup_path = self.board_path.parent / f"{self.board_path.stem}_backup_{datetime.now().strftime('%Y%m%d_%H%M%S')}.md"
    
    def backup_board(self):
        """备份当前看板"""
        if self.board_path.exists():
            content = self.board_path.read_text(encoding='utf-8')
            self.backup_path.write_text(content, encoding='utf-8')
            print(f"✓ 备份看板: {self.backup_path}")
    
    def query_db_status(self):
        """查询数据库最新任务状态"""
        conn = sqlite3.connect(self.db_path)
        cursor = conn.cursor()
        cursor.execute("""
            SELECT id, title, status, priority, estimated_hours, actual_hours
            FROM tasks
            ORDER BY status, priority, id
        """)
        tasks = {}
        for row in cursor.fetchall():
            task_id, title, status, priority, est, act = row
            tasks[task_id] = {
                'title': title,
                'status': status,
                'priority': priority,
                'estimated_hours': est or 0,
                'actual_hours': act or 0
            }
        conn.close()
        return tasks
    
    def parse_board(self):
        """解析当前看板，提取任务列表"""
        if not self.board_path.exists():
            return {}
        
        content = self.board_path.read_text(encoding='utf-8')
        # 提取所有任务ID
        pattern = r'(\w+-[\w-]+)\s*[✅🔄⏳❌]'
        matches = re.findall(pattern, content)
        return set(matches)
    
    def detect_changes(self, db_tasks, board_tasks):
        """检测变化"""
        changes = {
            'new_completed': [],  # 数据库中新完成的任务
            'new_pending': [],    # 数据库中新增的待处理任务
            'status_changed': [], # 状态变化的任务
            'not_in_board': []    # 数据库中有但看板没有的
        }
        
        for task_id, task_data in db_tasks.items():
            if task_id not in board_tasks:
                changes['not_in_board'].append((task_id, task_data))
        
        # 可以进一步解析看板中的任务状态，对比差异
        
        return changes
    
    def generate_update_report(self, changes):
        """生成更新报告"""
        print("\n" + "="*70)
        print("看板同步报告")
        print("="*70)
        print()
        print(f"  数据库中有但看板未提到: {len(changes['not_in_board'])}个")
        if changes['not_in_board']:
            print()
            print("  建议添加到看板:")
            for task_id, task_data in changes['not_in_board'][:10]:
                status_emoji = {
                    'completed': '✅',
                    'in_progress': '🔄',
                    'pending': '⏳',
                    'cancelled': '❌'
                }.get(task_data['status'], '?')
                print(f"    {status_emoji} {task_id}: {task_data['title'][:50]}...")
        print()
        print("="*70)
    
    def sync(self, dry_run=True):
        """执行同步"""
        print("\n开始同步看板...")
        print(f"  数据库: {self.db_path}")
        print(f"  看板: {self.board_path}")
        print(f"  模式: {'预览' if dry_run else '实际更新'}")
        print()
        
        # 1. 备份
        if not dry_run:
            self.backup_board()
        
        # 2. 查询数据库
        print("[1/3] 查询数据库状态...")
        db_tasks = self.query_db_status()
        print(f"  ✓ 数据库任务: {len(db_tasks)}个")
        
        # 3. 解析看板
        print("[2/3] 解析当前看板...")
        board_tasks = self.parse_board()
        print(f"  ✓ 看板任务: {len(board_tasks)}个")
        
        # 4. 检测变化
        print("[3/3] 检测变化...")
        changes = self.detect_changes(db_tasks, board_tasks)
        
        # 5. 生成报告
        self.generate_update_report(changes)
        
        if dry_run:
            print("\n⚠️ 这是预览模式，看板未实际更新")
            print("   运行 python xxx.py --apply 来应用更新")
        else:
            print("\n✅ 看板已更新")
        
        return changes

if __name__ == "__main__":
    import argparse
    parser = argparse.ArgumentParser()
    parser.add_argument('--apply', action='store_true', help='实际应用更新（默认只预览）')
    args = parser.parse_args()
    
    db_path = "database/data/tasks.db"
    board_path = "docs/tasks/task-board.md"
    
    syncer = TaskBoardAutoSync(db_path, board_path)
    syncer.sync(dry_run=not args.apply)
```

#### 使用方法

```bash
# 预览模式（只看变化，不修改）
python services/task_board_auto_sync.py

# 实际更新模式
python services/task_board_auto_sync.py --apply

# 定时运行（crontab）
*/10 * * * * cd /path/to/taskflow && python services/task_board_auto_sync.py --apply
```

#### 验收标准

- [ ] 脚本可以正常运行
- [ ] 预览模式显示所有不一致
- [ ] apply模式能正确更新看板
- [ ] 备份文件正确生成
- [ ] 看板格式保持不变
- [ ] 有清晰的日志输出

---

### 任务3: TASK-UI-002 - 完善Dashboard事件流Tab

**优先级**: P2（次要）  
**预估工时**: 3小时  
**执行者**: 全栈工程师·李明  
**依赖**: 无（基础版已存在，可以增量增强）

#### 当前状态

**已有**（基础版）:
- ✅ 事件流Tab存在（line 2725, 2741）
- ✅ 事件流API完整（7个端点）
- ✅ 基础列表展示

**缺少**（增强功能）:
- ❌ 筛选器（按类型/严重性/时间/操作者）
- ❌ 搜索功能
- ❌ 事件详情展开（查看完整data）
- ❌ 统计图表
- ❌ 实时刷新

#### 需要新增的功能

详见数据库中TASK-UI-002的description字段。

**重点**:
- 添加多维度筛选器
- 添加关键词搜索
- 事件详情可展开
- 统计数据可视化

---

## 🚀 派发方式

### 方式1: 直接复制任务描述

**给李明的消息**:

```markdown
@全栈工程师·李明

请执行以下任务（已录入数据库）：

**P1紧急（5小时）**:
1. TASK-UI-001: 实现Dashboard项目记忆空间Tab（3h）
2. TASK-AUTO-001: 实现自动化看板刷新脚本（2h）

**P2可选（3小时）**:
3. TASK-UI-002: 完善Dashboard事件流Tab（3h）

详细说明见：
- 数据库：SELECT description FROM tasks WHERE id='TASK-UI-001'
- 本文档：完整的实现要点和代码示例

开始前运行：
python scripts/李明收到任务.py TASK-UI-001

完成后运行：
python scripts/李明提交完成.py TASK-UI-001 --hours <实际>
```

### 方式2: 导出任务详情

```bash
# 导出TASK-UI-001详情
sqlite3 database/data/tasks.db "SELECT title, description, priority, estimated_hours FROM tasks WHERE id='TASK-UI-001';" > TASK-UI-001-详情.txt

# 发送给另一个Cursor窗口
```

---

## 📊 任务优先级

| 任务 | 优先级 | 工时 | 价值 | 难度 | 建议顺序 |
|------|--------|------|------|------|---------|
| TASK-UI-001 | P1 | 3h | ⭐⭐⭐⭐⭐ | 中 | 第1个 |
| TASK-AUTO-001 | P1 | 2h | ⭐⭐⭐⭐⭐ | 中 | 第2个 |
| TASK-UI-002 | P2 | 3h | ⭐⭐⭐⭐ | 低 | 第3个 |

**建议**: 先做TASK-UI-001和TASK-AUTO-001（5小时），TASK-UI-002可以稍后优化。

---

## 🎯 预期成果

### 完成TASK-UI-001后

- ✅ Dashboard新增"项目记忆空间"Tab
- ✅ 可以浏览历史记忆
- ✅ 可以创建新记忆
- ✅ 可以搜索和筛选
- ✅ 新架构师可以快速继承项目知识

**价值**: **打通跨会话知识继承！新架构师接手时可以自动获取历史经验！** ⭐⭐⭐⭐⭐

### 完成TASK-AUTO-001后

- ✅ 看板自动同步
- ✅ 不再需要手动更新
- ✅ 任务状态实时准确
- ✅ 再也不会"鬼打墙"

**价值**: **彻底解决"功能完成但看板不知道"的问题！** ⭐⭐⭐⭐⭐

---

## 📋 检查清单（派发前）

- [x] 任务已录入数据库
- [x] 任务详情已编写完整
- [x] 技术要点已说明
- [x] 代码示例已提供
- [x] 验收标准已明确
- [x] 派发文档已生成

**可以立即派发！** ✅

---

**派发人**: AI Architect  
**派发时间**: 2025-11-19 12:00  
**任务数**: 3个  
**总工时**: 8小时  
**预期完成**: 1个工作日

🎯 **就差这3个功能，整个系统就完美了！加油！**

