# 📖 架构师发任务完整指南

**适用环境**: 8831测试环境  
**目标角色**: AI架构师  
**接收角色**: 全栈工程师  
**版本**: v1.0

---

## 🎯 任务生命周期

```
架构师发起任务
      ↓
【待处理】 pending
      ↓ (工程师点击"接受任务")
【进行中】 in_progress  
      ↓ (工程师点击"提交完成")
【已完成】 completed
      ↓ (架构师审查)
【归档】 archived
```

---

## 📋 发任务完整流程

### 方式1: 通过数据库直接发任务（推荐）

#### 步骤1: 创建任务SQL
```sql
INSERT INTO tasks (
    id, title, description, status, priority, 
    estimated_hours, complexity, assigned_to, 
    created_at, updated_at, metadata
) VALUES (
    'TASK-FE-101',
    '实现用户个人中心页面',
    '创建用户个人中心页面，包括：
    
    【功能需求】
    1. 用户基本信息展示（头像、昵称、邮箱）
    2. 个人统计数据（任务数、对话数、Token使用）
    3. 最近活动列表
    4. 设置选项（主题、语言、通知）
    
    【技术要求】
    - 使用Blanc Luxury设计系统
    - 响应式布局
    - 数据从API动态加载
    
    【验收标准】
    - [ ] 所有信息正确显示
    - [ ] 设置可以保存
    - [ ] 移动端适配良好
    ',
    'pending',
    'P1',
    5.0,
    'medium',
    'fullstack-engineer',
    datetime('now'),
    datetime('now'),
    '{"tags": ["frontend", "ui", "profile"], "parallel": true}'
);
```

#### 步骤2: 执行SQL插入任务
```bash
cd /Users/yalinwang/Desktop/任务所\ 1.8/taskflow-v1-2/taskflow-v1-2

sqlite3 database/data/tasks.db < your_task.sql
```

#### 步骤3: 验证任务已创建
```bash
sqlite3 database/data/tasks.db "SELECT id, title, status FROM tasks WHERE id='TASK-FE-101'"
```

#### 步骤4: 通知工程师
复制并发送以下提示词给工程师：

---

### 方式2: 通过Python脚本发任务

创建文件 `scripts/architect_create_task.py`:

```python
#!/usr/bin/env python
# -*- coding: utf-8 -*-
import sqlite3
import json
from datetime import datetime
from pathlib import Path

def create_task(
    task_id: str,
    title: str,
    description: str,
    priority: str = "P1",
    estimated_hours: float = 3.0,
    complexity: str = "medium",
    tags: list = None,
    parallel: bool = False
):
    """架构师创建任务"""
    
    db_path = Path(__file__).parent.parent / "database" / "data" / "tasks.db"
    conn = sqlite3.connect(str(db_path))
    cursor = conn.cursor()
    
    metadata = {
        "tags": tags or [],
        "parallel": parallel,
        "created_by": "architect"
    }
    
    cursor.execute("""
        INSERT INTO tasks (
            id, title, description, status, priority,
            estimated_hours, complexity, assigned_to,
            created_at, updated_at, metadata
        ) VALUES (?, ?, ?, 'pending', ?, ?, ?, 'fullstack-engineer', 
                  datetime('now'), datetime('now'), ?)
    """, (
        task_id, title, description, priority,
        estimated_hours, complexity, json.dumps(metadata)
    ))
    
    conn.commit()
    conn.close()
    
    print(f"✅ 任务 {task_id} 已创建")
    print(f"   标题: {title}")
    print(f"   优先级: {priority}")
    print(f"   预估: {estimated_hours}小时")
    print(f"   可并行: {'是' if parallel else '否'}")

# 示例使用
if __name__ == "__main__":
    create_task(
        task_id="TASK-FE-101",
        title="实现用户个人中心页面",
        description="""创建用户个人中心页面...（详细描述）""",
        priority="P1",
        estimated_hours=5.0,
        complexity="medium",
        tags=["frontend", "ui", "profile"],
        parallel=True
    )
```

**执行**:
```bash
python3 scripts/architect_create_task.py
```

---

## 📝 任务提示词标准模板

当任务创建后，复制以下模板发给全栈工程师：

```markdown
# 📤 派发给全栈工程师 - {TASK_ID}

**派发时间**: {CURRENT_TIME}  
**派发人**: AI架构师 (Expert Level)  
**接收人**: 全栈工程师  
**优先级**: {PRIORITY}  
**预估工时**: {ESTIMATED_HOURS}小时  
**是否可并行**: {PARALLEL}

---

## 🚀 第一步：接受任务（必做！）

⚠️ **重要**: 看到此任务后，请：

1. 打开 Dashboard: http://localhost:8831
2. 进入"全栈工程师工作台" → "任务看板" Tab
3. 找到任务 `{TASK_ID}`
4. 点击"✅ 接受任务"按钮

**系统会自动**:
- ✅ 更新任务状态: 待处理 → 进行中
- ✅ 记录接收时间
- ✅ Dashboard实时显示

**⚠️ 不接受任务，系统无法追踪进度！**

---

## 📋 任务详情

### 任务描述
{TASK_DESCRIPTION}

### 技术要求
{TECHNICAL_REQUIREMENTS}

### 验收标准
- [ ] 功能完整实现
- [ ] 代码质量良好
- [ ] 自测通过
- [ ] 无明显Bug
- [ ] 文档完整

---

## 🔧 技术参考

**相关文档**:
- `docs/ai/fullstack-engineer-system-prompt.md`
- `docs/design/blanc-luxury-design-system.md`
- `{RELEVANT_DOCS}`

**相关代码**:
- `{RELEVANT_FILES}`

---

## 🧪 自测清单

**提交前必须检查**:
- [ ] 功能完整实现
- [ ] 代码已格式化
- [ ] 无Console错误
- [ ] 无Linter警告
- [ ] 已在本地测试
- [ ] 代码已提交Git

---

## 📝 完成后提交（必做！）

完成开发后，请：

1. 打开 Dashboard: http://localhost:8831
2. 进入"全栈工程师工作台" → "任务看板" Tab
3. 找到任务 `{TASK_ID}`（现在应该在"进行中"栏）
4. 点击"✅ 提交完成"按钮
5. 填写：
   - 实际工时（小时）
   - 完成说明

**系统会自动**:
- ✅ 更新任务状态: 进行中 → 已完成
- ✅ 记录完成时间和工时
- ✅ 生成完成报告

**然后**: 
1. 刷新Dashboard
2. 在"已完成"栏找到任务
3. 点击"复制完成报告"按钮
4. 粘贴报告到新对话
5. 补充详细信息后提交给架构师审查

---

## 🎯 重要提醒

### ✅ 必须做的事
1. **接受任务前**: 仔细阅读任务描述和技术要求
2. **开发过程中**: 遇到问题及时沟通
3. **提交前**: 完成自测清单的所有项
4. **提交后**: 及时复制完成报告给架构师

### ❌ 不要做的事
1. 不要跳过"接受任务"步骤
2. 不要在未完成时提交
3. 不要忽略验收标准
4. 不要忘记更新文档

---

**祝开发顺利！** 💪

**架构师**: AI Architect (Expert Level)  
**创建时间**: {CURRENT_TIME}
```

---

## 🎨 提示词变量替换表

使用时替换以下变量：

| 变量 | 说明 | 示例 |
|------|------|------|
| `{TASK_ID}` | 任务ID | TASK-FE-101 |
| `{CURRENT_TIME}` | 当前时间 | 2025-11-22 15:45 |
| `{PRIORITY}` | 优先级 | P0/P1/P2 |
| `{ESTIMATED_HOURS}` | 预估工时 | 5.0 |
| `{PARALLEL}` | 是否可并行 | 是/否 |
| `{TASK_DESCRIPTION}` | 任务描述 | 详细的功能说明 |
| `{TECHNICAL_REQUIREMENTS}` | 技术要求 | 框架、规范等 |
| `{RELEVANT_DOCS}` | 相关文档 | 文档路径列表 |
| `{RELEVANT_FILES}` | 相关文件 | 代码文件列表 |

---

## 📊 任务优先级说明

| 优先级 | 含义 | 响应时间 | 适用场景 |
|--------|------|---------|---------|
| **P0** | 紧急致命 | 立即处理 | 线上故障、阻塞其他任务 |
| **P1** | 重要紧急 | 当天完成 | 核心功能、MVP必需 |
| **P2** | 重要不紧急 | 本周完成 | 优化改进、次要功能 |
| **P3** | 不重要 | 排期处理 | 锦上添花、技术债 |

---

## 🔧 任务类型分类

### Frontend（前端）
- UI组件开发
- 页面实现
- 交互逻辑
- 样式优化

### Backend（后端）
- API开发
- 数据库操作
- 业务逻辑
- 中间件

### Fullstack（全栈）
- 前后端联调
- 完整功能模块
- 数据流打通

### Refactor（重构）
- 代码优化
- 架构调整
- 性能优化

### Bugfix（修复）
- Bug修复
- 问题排查
- 紧急修复

### Test（测试）
- 单元测试
- 集成测试
- E2E测试

### Docs（文档）
- API文档
- 使用指南
- 技术文档

---

## 💡 发任务最佳实践

### ✅ 好的任务描述
```
【清晰】实现用户登录功能
【详细】包括账号密码登录和第三方登录（微信、GitHub）
【标准】采用JWT认证，Token有效期7天
【验收】登录成功跳转首页，失败显示错误提示
```

### ❌ 不好的任务描述
```
【模糊】做个登录
【笼统】实现认证功能  
【缺失】登录页面（没有技术要求）
```

### 任务拆分原则
1. **单一职责**: 一个任务只做一件事
2. **工时适中**: 3-8小时为宜，不超过1天
3. **可验收**: 明确的验收标准
4. **独立性**: 尽量减少任务间依赖

---

## 🎯 可并行任务标记规则

### 什么任务可以并行？

✅ **可以并行**:
- 独立的UI组件开发
- 不同模块的功能开发
- 独立的API端点开发
- 文档编写
- 测试用例编写

❌ **不可并行**:
- 有强依赖关系的任务
- 修改同一文件的任务
- 需要按顺序执行的任务
- 数据库Schema修改

### 标记方法
在创建任务时，设置 `metadata`:
```json
{
  "parallel": true,
  "tags": ["frontend", "component", "独立"]
}
```

---

## 🚀 快速发任务示例

### 示例1: 前端UI任务（可并行）
```python
create_task(
    task_id="TASK-FE-102",
    title="实现任务卡片拖拽功能",
    description="""
    在任务看板实现拖拽排序功能。
    
    【技术栈】
    - HTML5 Drag and Drop API
    - 或使用 SortableJS 库
    
    【实现要点】
    1. 卡片可拖拽
    2. 拖拽时显示占位符
    3. 放下后更新顺序
    4. 调用API保存新顺序
    
    【验收】
    - 拖拽流畅无卡顿
    - 顺序持久化保存
    - 移动端也能用
    """,
    priority="P1",
    estimated_hours=4.0,
    complexity="medium",
    tags=["frontend", "interaction", "drag-drop"],
    parallel=True  # ✅ 可并行
)
```

### 示例2: 后端API任务（可并行）
```python
create_task(
    task_id="TASK-BE-201",
    title="实现文件上传API",
    description="""
    创建文件上传API端点。
    
    【端点设计】
    POST /api/upload
    - 支持多文件上传
    - 限制：单文件≤10MB，总计≤50MB
    - 支持类型：图片、PDF、文档
    
    【技术实现】
    - 使用FastAPI UploadFile
    - 文件存储到 uploads/ 目录
    - 返回文件URL和元数据
    - 实现进度回调
    
    【安全】
    - 文件类型验证
    - 大小限制
    - 文件名消毒（防止路径穿越）
    """,
    priority="P2",
    estimated_hours=3.0,
    complexity="medium",
    tags=["backend", "api", "upload"],
    parallel=True  # ✅ 可并行
)
```

### 示例3: 全栈功能（不可并行）
```python
create_task(
    task_id="TASK-FS-301",
    title="实现实时通知系统",
    description="""
    实现WebSocket实时通知功能。
    
    【前端】
    - WebSocket连接管理
    - 通知组件（Toast提示）
    - 未读消息徽章
    
    【后端】
    - WebSocket服务端
    - 消息推送逻辑
    - 连接管理和心跳
    
    【联调】
    - 前后端WebSocket协议对齐
    - 消息格式统一
    - 断线重连测试
    """,
    priority="P0",
    estimated_hours=8.0,
    complexity="high",
    tags=["fullstack", "websocket", "realtime"],
    parallel=False  # ❌ 不可并行（涉及架构级修改）
)
```

---

## 📖 任务提示词模板（完整版）

保存为 `templates/task-prompt-fullstack.md`:

```markdown
# 📤 派发给全栈工程师 - {TASK_ID}

**派发时间**: 2025-11-22 15:45  
**派发人**: AI架构师 (Expert Level)  
**接收人**: 全栈工程师  
**优先级**: {PRIORITY}  
**预估工时**: {HOURS}小时  
**复杂度**: {COMPLEXITY}  
**是否可并行**: {PARALLEL}

---

## 🚀 第一步：接受任务（必做！）

请打开 Dashboard: http://localhost:8831

1. 进入"全栈工程师工作台"
2. 点击"任务看板" Tab
3. 找到任务卡片 `{TASK_ID}`
4. 点击"✅ 接受任务"按钮

系统会自动将任务状态从"待处理"更新为"进行中"。

---

## 📋 任务详情

### 任务描述
{DESCRIPTION}

### 功能需求
{REQUIREMENTS}

### 技术要求
{TECH_SPECS}

### 验收标准
- [ ] {CRITERION_1}
- [ ] {CRITERION_2}
- [ ] {CRITERION_3}

---

## 🔧 技术参考

**设计系统**:
- Blanc Luxury设计规范
- 12级白色层次
- 响应式布局标准

**相关文档**:
- `{DOC_PATH_1}`
- `{DOC_PATH_2}`

**相关代码**:
- `{CODE_PATH_1}`
- `{CODE_PATH_2}`

---

## 🧪 自测清单

提交前必须检查：
- [ ] 功能完整实现
- [ ] 代码已格式化
- [ ] 无Console错误
- [ ] 无Linter警告
- [ ] 已在本地测试
- [ ] 响应式适配OK
- [ ] 代码已提交Git
- [ ] 文档已更新

---

## 📝 完成后提交（必做！）

### 步骤1: 提交完成
1. 打开 Dashboard: http://localhost:8831
2. 进入"全栈工程师工作台" → "任务看板"
3. 找到任务（现在在"进行中"栏）
4. 点击"✅ 提交完成"按钮
5. 填写：
   - 实际工时: ____ 小时
   - 完成说明: ____

### 步骤2: 复制完成报告
1. 刷新Dashboard
2. 任务移到"已完成"栏
3. 点击"复制完成报告"按钮
4. 粘贴到新对话

### 步骤3: 提交给架构师
在完成报告基础上，补充：
- 实现的具体功能
- 创建/修改的文件
- 遇到的问题和解决方案
- 代码行数统计
- 测试结果截图

---

## 💡 开发提示

### 如果遇到问题
1. 查看相关文档
2. 参考类似功能的实现
3. 咨询架构师
4. 查看事件流寻找线索

### 如果任务太大
请反馈给架构师，可以拆分为多个子任务。

### 如果发现需求不清晰
请及时提出，架构师会补充说明。

---

**祝开发顺利！** 💪

**架构师**: AI Architect (Expert Level)  
**创建时间**: 2025-11-22
```

---

## 🎯 架构师常用命令速查

```bash
# 查看待处理任务数
sqlite3 database/data/tasks.db "SELECT COUNT(*) FROM tasks WHERE assigned_to='fullstack-engineer' AND status='pending'"

# 查看进行中任务
sqlite3 database/data/tasks.db "SELECT id, title, updated_at FROM tasks WHERE status='in_progress'"

# 查看最近完成的任务
sqlite3 database/data/tasks.db "SELECT id, title, actual_hours FROM tasks WHERE status='completed' ORDER BY completed_at DESC LIMIT 5"

# 统计工程师工作量
sqlite3 database/data/tasks.db "SELECT status, COUNT(*), SUM(estimated_hours) FROM tasks WHERE assigned_to='fullstack-engineer' GROUP BY status"
```

---

## 📞 联系方式

**问题反馈**: 在对话中 @架构师  
**紧急事项**: Dashboard留言板  
**技术讨论**: 架构师工作台 → 对话历史

---

**文档版本**: v1.0  
**创建时间**: 2025-11-22  
**维护人**: AI架构师

