# 📍 端口说明 - 重要！

## 🎯 当前端口分配

### Dashboard前端（静态HTML）
- **8831** - 测试环境 (dashboard-test-8831/)
- **8820** - 生产环境 (dashboard-v1.9-20251121/)

### API后端服务

#### 8800端口 - 透视塔API（已运行）
```bash
python3 start_insight_api.py
```
**提供的端点**:
- `/api/features/implemented` - 已实现功能
- `/api/features/partial` - 部分实现功能
- `/api/issues` - 问题清单
- `/api/suggestions` - 架构建议
- `/api/architect/events` - 架构师事件
- `/api/architect/monitor` - 架构师监控
- `/api/architect/conversations` - 架构师对话
- `/api/pulse/events` - 实时脉动事件

#### 8000端口 - 完整API（记忆模块需要）
```bash
cd apps/api
uvicorn src.main:app --reload --port 8000
```
**提供的端点**:
- `/api/projects/TASKFLOW/memories` - 记忆CRUD
- `/api/projects/TASKFLOW/memories/stats` - 记忆统计
- `/api/events/stream` - 事件流
- `/api/conversations` - 对话管理
- 等等...

---

## ⚠️ 重要区别

### 透视塔模块 → 使用8800端口
```javascript
fetch('http://localhost:8800/api/features/implemented')
```

### 记忆空间模块 → 使用8000端口
```javascript
fetch('http://localhost:8000/api/projects/TASKFLOW/memories/stats')
```

---

## 🚀 当前部署状态

### ✅ 已完成
- 8831测试环境：记忆模块代码已部署
- 8820生产环境：透视塔模块已部署

### ⏳ 待启动（如需测试记忆API）
```bash
# 启动完整API服务（8000端口）
cd "/Users/yalinwang/Desktop/任务所 1.8/taskflow-v1-2/taskflow-v1-2/apps/api"
uvicorn src.main:app --reload --port 8000
```

### 📍 当前可测试的功能

#### 在8831测试（不需要8000 API）
- ✅ 搜索框（DOM过滤）
- ✅ 创建按钮（弹出对话框）
- ✅ 筛选按钮（DOM过滤）
- ✅ UI完全匹配Windows

#### 需要8000 API才能测试
- ⏳ 统计数字动态更新
- ⏳ 记忆列表动态加载
- ⏳ 创建记忆提交到数据库
- ⏳ 事件流自动刷新

---

## 💡 建议

### 现在就可以验证的
1. 打开 http://localhost:8831
2. 检查UI是否和Windows一样
3. 测试搜索、筛选、对话框

### 稍后需要API的
- 如果要测试完整功能，需要启动8000端口API
- 如果只验证UI，现在就可以测试

---

**现在您可以访问 http://localhost:8831 验证UI是否完全匹配Windows版本！** 🎯

